// # 2025-09-21 16:58
#include <stdio.h>          // Include a header / library file

int main() {                // main() is a special function. Your program starts running here. Code will be executed in '{}'.
  printf("Hello, World!");  // printf is a function from standard input output library: <stdio.h>. This func will print text to terminal
  return 0;                 // ends the main() function and sends a value back to OS. return 0 means everything worked.
}

/*
 * A computer program is a list of instructions that your PC follows,
 * In C instructions are called statements.
 * Every statement must end with a semicolon `;`
*/

// Compile: gcc hello_world.c -o hello
// Run: ./hello
